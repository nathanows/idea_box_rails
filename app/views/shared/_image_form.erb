<%= form_for image, :url => images_path, :html => { :multipart => true  } do |f| %>
  <%= f.label          :title %>
  <%= f.text_field     :title %>
  <%= f.label          :img %>
  <%= f.file_field     :img %>

  <%= f.submit         "Add Image" %>
<% end %>
