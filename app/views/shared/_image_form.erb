<%= bootstrap_form_for image, layout: :horizontal, :url => images_path, :html => { :multipart => true  } do |f| %>
  <%= f.text_field     :title, control_col: "col-sm-6"%>
  <%= f.file_field     :img %>
  <%= f.submit         "Add Image" %>
<% end %>
